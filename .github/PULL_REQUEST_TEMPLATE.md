<!-- Link the issue this closes, so merging the pull request closes it:
     "Closes #12". If it is related but does not close it, write "Refs #12". -->
Closes #

## What this changes

<!-- One paragraph. What behaviour is different after this, and why. -->

## Checklist

- [ ] `python build.py && node smoke_test.js && node api/test_api.js` is green
- [ ] `src/` changed, so `index.html` was rebuilt and is committed (or `src/` was untouched)
- [ ] A dated entry was added to `DECISIONS.md` if this involved a design decision
- [ ] `SPEC.md` was updated if physics, a block model, or the roadmap changed
- [ ] `VALIDATION.md` was rechecked if a block's behaviour changed
- [ ] I have signed the CLA by adding my line to `CONTRIBUTORS.md` (first pull request only)

## How it was checked

<!-- Beyond the test suites: the case you ran, the reference you compared
     against, and how close it came. For a user interface change, say what you
     clicked and what you saw. -->
